[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "slsn-metrics"
version = "0.1.0"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["py_files"]
include = ["slsn_metrics*"]
